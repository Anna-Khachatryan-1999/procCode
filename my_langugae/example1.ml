#tivI n
#mutq <- n
#
#tivI s = 1
#ete(n >= 0) ->
##    elq -> 1
#<-
#
#cikl (n != 0) ->
#    s = (s * n)
#    n = (n - 1)
#    ete (n == 1) ->
#        elq -> [fghjk]
#    <-
#<-
#elq -> s
tivI t = 12 #v + qefqfvuq
elq -> t
